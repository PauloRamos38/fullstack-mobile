# Variáveis de Ambiente - PRODUÇÃO
# ⚠️ NUNCA commitar este arquivo!

# === ESSENCIAIS ===
NODE_ENV=production
PORT=3000

# === JWT (GERAR NOVOS SECRETS!) ===
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=GERAR_UM_SECRET_ALEATORIO_DE_32_CARACTERES_MINIMO
JWT_REFRESH_SECRET=GERAR_OUTRO_SECRET_DIFERENTE_DO_ANTERIOR
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# === DATABASE ===
DATABASE_URL=postgresql://user:password@host:5432/database

# === CORS ===
CORS_ORIGIN=https://seu-dominio.com

# === RATE LIMITING ===
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# === LOGGING (OPCIONAL) ===
SENTRY_DSN=
LOG_LEVEL=info

# === OUTROS ===
REDIS_URL=redis://localhost:6379
